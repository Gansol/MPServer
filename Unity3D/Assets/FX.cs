using UnityEngine;
using System.Collections;

public class FX : MonoBehaviour {

	// Use this for initialization
	void Start () {
        MissionManager m = GetComponent<MissionManager>();
        Debug.Log(m);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
